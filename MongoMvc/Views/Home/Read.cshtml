@{
    ViewData["Title"] = "Read all records";
}

@model IEnumerable<MongoMvc.Model.Note>

<h2>@ViewData["Title"].</h2>
    <ul>
        @foreach (var item in Model)
        {
            <li>@item.Id @item.Body</li>  
        }
    </ul>
